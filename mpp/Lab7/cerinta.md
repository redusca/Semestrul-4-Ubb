# Implementați partea de networking (folosind socketuri și threaduri) a proiectului  și notificarea clientilor (actualizarea automată a datelor afișate la clienți).

## Important:

## Pentru testare veți crea un proces pentru server și câte un proces pentru fiecare client. La prezentare veți folosi cel puțin 2 clienți.